elem-simple
==============
Simplest version of elem

* JSX
* functions as tag
* props
  * props.children
  * props.redraw
  * props.myself
  * props.context
* inline styling
* classes
  * class as string
  * class as array
  * class as object
* universal render
* ref callback
* predicate
