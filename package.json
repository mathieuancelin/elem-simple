{
  "name": "elem-simple",
  "version": "1.0.0",
  "description": "Simplest version of Elem",
  "main": "lib/index.js",
  "jsnext:main": "lib/index.js",
  "scripts": {
    "compress": "gzip --best -c ./dist/elem.js > ./dist/elem.gzip",
    "lines": "cloc src",
    "lint": "eslint src tests",
    "test": "NODE_ENV=test mocha --compilers js:babel-core/register tests/index.js",
    "test:watch": "npm test -- --watch",
    "test:cov": "NODE_ENV=test babel-node $(npm bin)/isparta cover $(npm bin)/_mocha tests/index.js",
    "test:cove": "NODE_ENV=test istanbul cover _mocha -- --compilers js:babel-core/register tests/index.js",
    "perfs": "NODE_ENV=test mocha --compilers js:babel-core/register tests/perfs.spec.js",
    "start": "NODE_ENV=dev webpack-dev-server -d --colors --hot --inline --progress",
    "bundle": "rm -rf ./dist && NODE_ENV=production webpack -p --colors --progress",
    "lib": "rm -rf ./lib && babel src --out-dir lib",
    "build": "npm run test && npm run lint && npm run lib && npm run bundle && npm run compress && npm run lines && ls -ahl ./dist",
    "build:travis": "npm run test && npm run lint && npm run lib && npm run bundle && test:cov"
  },
  "author": "Mathieu ANCELIN",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "babel": "^6.3.13",
    "babel-cli": "^6.3.17",
    "babel-core": "^6.3.21",
    "babel-eslint": "^5.0.0-beta6",
    "babel-loader": "^6.2.0",
    "babel-preset-es2015": "^6.3.13",
    "babel-preset-react": "^6.3.13",
    "babel-preset-stage-0": "^6.3.13",
    "chai": "^3.4.1",
    "eslint": "^1.10.3",
    "eslint-config-airbnb": "^2.1.1",
    "eslint-plugin-react": "^3.11.3",
    "isparta": "^4.0.0",
    "istanbul": "^0.4.1",
    "jsdom": "^7.2.1",
    "mocha": "^2.3.4",
    "webpack": "^1.12.9",
    "webpack-dev-server": "^1.14.0"
  }
}
